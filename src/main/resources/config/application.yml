server:
    port: ${PORT:5000}

base-url: /api/

spring:
    application:
        name: taskManager
    profiles:
        active: ${APP_ENV}

    liquibase:
        change-log: classpath:db/changelog/changelog-master.yml
        
